let initialNavbarPage = {
    friends: [
        {id: 1, name: 'Dimych'},
        {id: 2, name: 'Pavel'},
        {id: 3, name: 'Yana'}
    ]
}

const navbarReducer = (state = initialNavbarPage, action) => {

    return state
}

export default navbarReducer